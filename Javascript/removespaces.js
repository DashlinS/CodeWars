/*Simple, remove the spaces from the string, then return the resultant string.

Starting Fuction 
function noSpace(x){
}

replace method used
.replace(/\s/g,'')
*/

function noSpace(x) {
  str = (x);
return str.replace(/\s/g,'');
}
